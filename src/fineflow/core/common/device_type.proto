syntax = "proto3";
package fineflow;

enum DeviceType {
  kInvalidDevice = 0;
  kCPU = 1;
  kCUDA = 2;
  kMockDevice = 3;  // pseudo device for test.
  kMetal = 4;       // Apple Metal (macOS/iOS)
  kROCm = 5;        // AMD ROCm
  kVulkan = 6;       // Vulkan
}
